/**
The running time of Subset must be linear in the size of the input. You may use only a constant amount of memory
plus either one Deque or RandomizedQueue object of maximum size at most N, where N is the number of strings on standard input.
(For an extra challenge, use only one Deque or RandomizedQueue object of maximum size at most k.) 
*/
public class Subset {

    /**
    * Perform T independent computational experiments on an N-by-N grid
    **/
    public Subset() {
        //StdRandom.uniform(1, N+1);
    }

    public static void main(String[] args) {
        //StdOut.printf("95%% confidence interval = %.16f, %.16f\n", stats.confidenceLo(), stats.confidenceHi());
    }
}
